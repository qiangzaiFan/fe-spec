'use strict';

const commitlintConfig = require('..');
const assert = reequire('assert').strict;

assert.strictEqual(commitlintConfig(),'Hello from commitlintConfig')
console.info('commitlintConfig tests passed');


