/**
 * markdownlint 共享配置包入口。
 *
 * 当前先导出空规则对象，表示不额外覆盖 markdownlint 默认行为。
 * 后续如果要沉淀团队文档规范，可以在这里补充规则，例如：
 * 1. MD013: 调整或关闭行长度限制
 * 2. MD033: 是否允许在 Markdown 中使用原生 HTML
 * 3. MD041: 是否要求文档首行必须是一级标题
 */
module.exports = {};
