{
  "name": "fe-spec-replica",
  "private": true,
  "packageManager": "pnpm@10.6.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "docs:dev": "echo \"TODO: wire docs dev server\"",
    "docs:build": "echo \"TODO: wire docs build\""
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}

