{
  "name": "markdownlint-config-encode",
  "version": "0.0.0",
  "private": true,
  "description": "Replica skeleton of markdownlint-config-encode",
  "main": "index.json",
  "scripts": {
    "build": "node -e \"console.log('markdownlint-config: nothing to build')\"",
    "test": "node --test ./test/*.test.cjs",
    "lint": "node -e \"console.log('markdownlint-config: lint not wired yet')\""
  }
}

