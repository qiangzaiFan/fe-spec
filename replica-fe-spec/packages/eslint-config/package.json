{
  "name": "eslint-config-encode",
  "version": "0.0.0",
  "private": true,
  "description": "Replica skeleton of eslint-config-encode",
  "main": "index.js",
  "scripts": {
    "build": "node -e \"console.log('eslint-config: nothing to build')\"",
    "test": "node --test ./test/*.test.js",
    "lint": "node -e \"console.log('eslint-config: lint not wired yet')\""
  }
}

