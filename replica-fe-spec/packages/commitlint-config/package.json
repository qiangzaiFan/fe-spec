{
  "name": "commitlint-config-encode",
  "version": "0.0.0",
  "private": true,
  "description": "Replica skeleton of commitlint-config-encode",
  "main": "index.cjs",
  "scripts": {
    "build": "node -e \"console.log('commitlint-config: nothing to build')\"",
    "test": "node --test ./test/*.test.cjs",
    "lint": "node -e \"console.log('commitlint-config: lint not wired yet')\""
  }
}

